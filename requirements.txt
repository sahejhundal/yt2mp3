# The [default] extra pulls in yt-dlp-ejs, needed (together with a JS runtime
# such as Deno) to solve YouTube's signature / n-sig challenges. Without it
# some tracks return only storyboard images.
yt-dlp[default]
ytmusicapi
certifi
# Used by dedupe_audio.py to read bitrate/tags when choosing which copy to keep.
mutagen
# Used by audio_quality.py for spectral (FFT) analysis of real audio quality.
numpy
